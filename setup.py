from setuptools import find_packages, setup

setup(
    name="neural-vol-surface",
    version="0.1.0",
    description="Institutional-grade neural volatility surface forecasting platform",
    packages=find_packages(),
    python_requires=">=3.11",
    install_requires=[],
)
